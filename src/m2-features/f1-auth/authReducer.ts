


type InitialStateType = typeof initialState

const initialState = {}

export const authReducer = (state: InitialStateType, action: any) => {
    switch (action.type) {
        default:
            return state
    }
}